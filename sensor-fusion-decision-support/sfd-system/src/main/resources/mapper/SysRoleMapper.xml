<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sursoft.sfd.system.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.sursoft.sfd.system.domain.SysRole">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sys_role WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND role_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_role WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert">
        INSERT INTO sys_role (id, role_name, role_code, description, status, is_deleted,
                              created_at, created_by)
        VALUES (#{id}, #{roleName}, #{roleCode}, #{description}, #{status}, 0, NOW(), #{createdBy})
    </insert>

    <update id="update">
        UPDATE sys_role SET role_name = #{roleName}, description = #{description},
            updated_at = NOW(), updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE sys_role SET is_deleted = 1, updated_at = NOW() WHERE id = #{id}
    </update>

    <select id="countUsersByRoleId" resultType="int">
        SELECT COUNT(*) FROM sys_user_role WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleMenus">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu (role_id, menu_id) VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <select id="selectMenuIdsByRoleId" resultType="long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>
</mapper>
