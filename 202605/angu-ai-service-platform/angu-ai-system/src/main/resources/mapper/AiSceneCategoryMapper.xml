<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.angu.ai.system.mapper.AiSceneCategoryMapper">

    <select id="selectAll" resultType="com.angu.ai.system.domain.entity.AiSceneCategory">
        SELECT * FROM ai_scene_category WHERE deleted = 0 ORDER BY sort, id
    </select>

    <select id="selectById" resultType="com.angu.ai.system.domain.entity.AiSceneCategory">
        SELECT * FROM ai_scene_category WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_scene_category (name, icon, sort, deleted) VALUES (#{name}, #{icon}, #{sort}, 0)
    </insert>

    <update id="updateById">
        UPDATE ai_scene_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE ai_scene_category SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="countScenes" resultType="int">
        SELECT COUNT(*) FROM ai_scene WHERE category_id = #{categoryId} AND deleted = 0
    </select>
</mapper>
