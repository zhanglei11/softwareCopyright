<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sva.system.mapper.SysMenuMapper">
    <resultMap id="BaseRM" type="com.sva.system.domain.SysMenu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_name" property="menuName"/>
        <result column="menu_type" property="menuType"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="perms" property="perms"/>
        <result column="icon" property="icon"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="visible" property="visible"/>
        <result column="status" property="status"/>
    </resultMap>
    <select id="selectAll" resultMap="BaseRM">SELECT * FROM sys_menu WHERE status=1 ORDER BY sort_order,id</select>
    <select id="selectById" resultMap="BaseRM">SELECT * FROM sys_menu WHERE id=#{id}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu(parent_id,menu_name,menu_type,path,component,perms,icon,sort_order,visible,status)
        VALUES(#{parentId},#{menuName},#{menuType},#{path},#{component},#{perms},#{icon},#{sortOrder},#{visible},#{status})
    </insert>
    <update id="update">
        UPDATE sys_menu SET parent_id=#{parentId},menu_name=#{menuName},menu_type=#{menuType},
        path=#{path},component=#{component},perms=#{perms},icon=#{icon},sort_order=#{sortOrder},
        visible=#{visible},status=#{status} WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM sys_menu WHERE id=#{id}</delete>
    <select id="countChildrenByParentId" resultType="long">SELECT COUNT(*) FROM sys_menu WHERE parent_id=#{parentId}</select>
</mapper>
