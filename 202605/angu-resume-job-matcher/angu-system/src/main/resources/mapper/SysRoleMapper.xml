<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.angu.matcher.system.mapper.SysRoleMapper">

    <resultMap id="roleMap" type="com.angu.matcher.system.domain.SysRole">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleCode" column="role_code"/>
        <result property="builtin" column="builtin"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectAll" resultMap="roleMap">
        SELECT * FROM sys_role ORDER BY id
    </select>

    <select id="selectById" resultMap="roleMap">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <select id="selectByRoleCode" resultMap="roleMap">
        SELECT * FROM sys_role WHERE role_code = #{roleCode}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(role_name, role_code, builtin, status, remark)
        VALUES(#{roleName}, #{roleCode}, 0, #{status}, #{remark})
    </insert>

    <update id="update">
        UPDATE sys_role SET role_name=#{roleName}, status=#{status}, remark=#{remark}
        WHERE id=#{id} AND builtin=0
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id=#{id} AND builtin=0
    </delete>

    <select id="countUserByRoleId" resultType="int">
        SELECT COUNT(*) FROM sys_user_role WHERE role_id = #{roleId}
    </select>

    <select id="selectMenuIdsByRoleId" resultType="long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleMenus">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu(role_id, menu_id) VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <select id="selectPermCodesByUserId" resultType="string">
        SELECT DISTINCT m.perm_code FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.perm_code IS NOT NULL AND m.perm_code != ''
    </select>

    <select id="selectRolesByUserId" resultMap="roleMap">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>
