<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vqcc.system.mapper.SysMenuMapper">
    <resultMap id="BaseResultMap" type="com.vqcc.system.domain.SysMenu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_name" property="menuName"/>
        <result column="menu_type" property="menuType"/>
        <result column="path" property="path"/>
        <result column="permission" property="permission"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
    </resultMap>
    <select id="selectAll" resultMap="BaseResultMap">SELECT * FROM sys_menu WHERE status=1 ORDER BY sort,id</select>
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON rm.menu_id=m.id
        INNER JOIN sys_user_role ur ON ur.role_id=rm.role_id
        WHERE ur.user_id=#{userId} AND m.status=1 ORDER BY m.sort,m.id
    </select>
    <select id="selectById" resultMap="BaseResultMap">SELECT * FROM sys_menu WHERE id=#{id}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu(parent_id,menu_name,menu_type,path,permission,icon,sort,status)
        VALUES(#{parentId},#{menuName},#{menuType},#{path},#{permission},#{icon},#{sort},#{status})
    </insert>
    <update id="update">
        UPDATE sys_menu <set>
        <if test="menuName!=null">menu_name=#{menuName},</if>
        <if test="path!=null">path=#{path},</if>
        <if test="permission!=null">permission=#{permission},</if>
        <if test="icon!=null">icon=#{icon},</if>
        <if test="sort!=null">sort=#{sort},</if>
        <if test="status!=null">status=#{status},</if>
        id=id</set> WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM sys_menu WHERE id=#{id}</delete>
</mapper>
