<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sva.system.mapper.SysRoleMapper">
    <resultMap id="BaseRM" type="com.sva.system.domain.SysRole">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>
    <select id="selectList" resultMap="BaseRM">
        SELECT r.*, COUNT(ur.user_id) AS userCount FROM sys_role r
        LEFT JOIN sys_user_role ur ON r.id=ur.role_id
        WHERE 1=1
        <if test="roleName != null and roleName != ''">AND r.role_name LIKE CONCAT('%',#{roleName},'%')</if>
        <if test="status != null">AND r.status=#{status}</if>
        GROUP BY r.id ORDER BY r.created_at DESC
    </select>
    <select id="selectById" resultMap="BaseRM">SELECT * FROM sys_role WHERE id=#{id}</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(role_name,role_code,description,status,created_at) VALUES(#{roleName},#{roleCode},#{description},#{status},NOW())
    </insert>
    <update id="update">UPDATE sys_role SET role_name=#{roleName},role_code=#{roleCode},description=#{description},status=#{status} WHERE id=#{id}</update>
    <delete id="deleteById">DELETE FROM sys_role WHERE id=#{id}</delete>
    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu(role_id,menu_id) VALUES
        <foreach collection="menuIds" item="mid" separator=",">(#{roleId},#{mid})</foreach>
    </insert>
    <delete id="deleteRoleMenus">DELETE FROM sys_role_menu WHERE role_id=#{roleId}</delete>
    <select id="selectMenuIdsByRoleId" resultType="long">SELECT menu_id FROM sys_role_menu WHERE role_id=#{roleId}</select>
    <select id="countUsersByRoleId" resultType="long">SELECT COUNT(*) FROM sys_user_role WHERE role_id=#{roleId}</select>
</mapper>
