<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imaging.scheduler.system.mapper.scene.SceneGroupMapper">

    <resultMap id="SceneGroupMap" type="com.imaging.scheduler.system.domain.scene.SceneGroup">
        <id column="id" property="id"/>
        <result column="group_name" property="groupName"/>
        <result column="description" property="remark"/>
        <result column="deleted" property="isDeleted"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectList" resultMap="SceneGroupMap">
        SELECT * FROM scene_group WHERE deleted = 0 ORDER BY sort_order ASC
    </select>

    <select id="selectById" resultMap="SceneGroupMap">
        SELECT * FROM scene_group WHERE id = #{id} AND deleted = 0
    </select>

    <select id="countSceneByGroupId" resultType="int">
        SELECT COUNT(*) FROM scene_info WHERE group_id = #{groupId} AND deleted = 0
    </select>

    <select id="countByGroupName" resultType="int">
        SELECT COUNT(*) FROM scene_group
        WHERE group_name = #{groupName} AND deleted = 0
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO scene_group(group_name, description, deleted, created_at, updated_at)
        VALUES(#{groupName}, #{remark}, 0, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE scene_group
        <set>
            <if test="groupName != null">group_name = #{groupName},</if>
            <if test="remark != null">description = #{remark},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE scene_group SET deleted = 1, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
