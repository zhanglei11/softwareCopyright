<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imaging.scheduler.system.mapper.dispatch.DispatchLogMapper">

    <sql id="queryWhere">
        WHERE 1=1
        <if test="taskId != null">AND l.task_id = #{taskId}</if>
        <if test="action != null and action != ''">AND l.action = #{action}</if>
        <if test="operatorId != null">AND l.operator_id = #{operatorId}</if>
    </sql>

    <select id="selectList" resultType="com.imaging.scheduler.system.domain.dispatch.DispatchLog">
        SELECT l.*, t.task_name FROM dispatch_log l
        LEFT JOIN task_info t ON l.task_id = t.id
        <include refid="queryWhere"/>
        ORDER BY l.created_at DESC
    </select>

    <select id="selectAllForExport" resultType="com.imaging.scheduler.system.domain.dispatch.DispatchLog">
        SELECT l.*, t.task_name FROM dispatch_log l
        LEFT JOIN task_info t ON l.task_id = t.id
        <include refid="queryWhere"/>
        ORDER BY l.created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dispatch_log(task_id, action, action_desc, device_ids, operator_id, operator_name, created_at)
        VALUES(#{taskId}, #{action}, #{actionDesc}, #{deviceIds}, #{operatorId}, #{operatorName}, NOW())
    </insert>
</mapper>
