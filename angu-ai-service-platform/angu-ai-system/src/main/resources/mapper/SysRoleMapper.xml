<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.angu.ai.system.mapper.SysRoleMapper">

    <select id="selectAll" resultType="com.angu.ai.system.domain.vo.RoleVO">
        SELECT id, role_name, role_code, status, description FROM sys_role ORDER BY id
    </select>

    <select id="selectById" resultType="com.angu.ai.system.domain.vo.RoleVO">
        SELECT id, role_name, role_code, builtin, status, description FROM sys_role WHERE id = #{id}
    </select>

    <select id="selectEntityById" resultType="com.angu.ai.system.domain.entity.SysRole">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (role_name, role_code, builtin, status, description)
        VALUES (#{roleName}, #{roleCode}, 0, 1, #{description})
    </insert>

    <update id="updateById">
        UPDATE sys_role
        <set>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.role_code FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

    <select id="selectPermsByUserId" resultType="string">
        SELECT DISTINCT m.perms FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_role r ON r.id = rm.role_id
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.perms IS NOT NULL AND m.perms != ''
          AND m.status = 1 AND r.status = 1
    </select>

    <insert id="insertRoleMenu">
        INSERT INTO sys_role_menu (role_id, menu_id) VALUES (#{roleId}, #{menuId})
    </insert>

    <delete id="deleteRoleMenus">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRoleScene">
        INSERT INTO sys_role_scene (role_id, scene_id) VALUES (#{roleId}, #{sceneId})
    </insert>

    <delete id="deleteRoleScenes">
        DELETE FROM sys_role_scene WHERE role_id = #{roleId}
    </delete>

    <select id="selectSceneIdsByUserId" resultType="long">
        SELECT DISTINCT rs.scene_id FROM sys_role_scene rs
        INNER JOIN sys_user_role ur ON ur.role_id = rs.role_id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>
