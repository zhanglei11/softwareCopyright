<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.angu.matcher.system.mapper.SysUserMapper">

    <resultMap id="userMap" type="com.angu.matcher.system.domain.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="errorCount" column="error_count"/>
        <result property="lockedUntil" column="locked_until"/>
        <result property="deleted" column="deleted"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="selectList" resultMap="userMap">
        SELECT * FROM sys_user WHERE deleted = 0
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_time DESC
    </select>

    <select id="selectById" resultMap="userMap">
        SELECT * FROM sys_user WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByUsername" resultMap="userMap">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username, real_name, phone, password, status, error_count, deleted, created_time, updated_time)
        VALUES(#{username}, #{realName}, #{phone}, #{password}, #{status}, 0, 0, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE sys_user SET updated_time = NOW()
        <if test="realName != null">
            , real_name = #{realName}
        </if>
        <if test="phone != null">
            , phone = #{phone}
        </if>
        <if test="status != null">
            , status = #{status}
        </if>
        <if test="password != null">
            , password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE sys_user SET deleted = 1, updated_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateErrorCount">
        UPDATE sys_user SET error_count = #{errorCount}, locked_until = #{lockedUntil}, updated_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
    </select>

    <delete id="deleteUserRoles">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserRoles">
        INSERT INTO sys_user_role(user_id, role_id) VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

</mapper>
