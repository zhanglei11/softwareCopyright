package com.angu.matcher.admin;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.angu.matcher")
public class AnguResumeJobMatcherApplication {
    public static void main(String[] args) {
        SpringApplication.run(AnguResumeJobMatcherApplication.class, args);
    }
}
